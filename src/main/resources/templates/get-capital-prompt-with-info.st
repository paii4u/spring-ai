what is the capital of {stateOrCountry}? Respond with the following format:
            The capital of `stateOrCountry` is `capital`.
            The currency used is `currency`.
            The language spoken is `language`.